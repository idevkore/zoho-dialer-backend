$kind: http-request
name: Health Check (/api/v1)
description: Duplicate health under API prefix for Nginx `/api/*` routing (`src/routes/index.js`).
url: "{{baseUrl}}/api/v1/health?tenantId=kore"
method: GET
queryParams:
  tenantId: kore
order: 2000
